const Button = () => {
  return (
    <button className="relative text-btn border-[3px] border-primary bg-primary my-5 py-1 px-10  shadow-custom">
      JOIN US
    </button>
  );
};

export default Button;
